% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai_story.R
\name{get_ai_story}
\alias{get_ai_story}
\title{Ask an LLM to tell the story of the decorated outputs}
\usage{
get_ai_story(
  outputs,
  allowed_layouts,
  platform = "deepseek",
  base_url = "https://api.deepseek.com",
  api_key = get_deepseek_key(),
  model = "deepseek-chat",
  max_slides = 4L
)
}
\arguments{
\item{outputs}{A named `list` of decorated outputs (see [decorate_outputs()]).}

\item{allowed_layouts}{Character vector of permitted layouts. Any layout the
model returns outside this set is clamped to `"Title and Content"`.}

\item{platform, base_url, api_key, model}{Passed to [get_ellmer_chat()] to build
the chat connection.}

\item{max_slides}{Integer cap on slides per section.}
}
\value{
A `list` with two elements, `summary` and `conclusions`, each a list
  of slides (`list(layout, title, bullets)`).
}
\description{
Sends the deck's tables to the chosen LLM provider and returns a structured
narrative: a `summary` section (for the front of the deck) and a
`conclusions` section (for the end). Each section is a list of slides, and
each slide has a `layout`, a `title` and a character vector of `bullets`.
}
